{
  "name": "ChromeExtensionEmailTracker",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "deps": "npm install",
    "stage": "mkdir dist || echo Dist already exists.",
    "bundle": "browserify -o dist/extension.js src/extension.js",
    "build": "npm run stage && npm run bundle"
  },
  "author": "Fuzail Gilani",
  "license": "ISC",
  "dependencies": {
    "gmail-js": "^0.6.13",
    "jquery": "^3.3.1"
  },
  "devDependencies": {
    "browserify": "^13.3.0"
  }
}
